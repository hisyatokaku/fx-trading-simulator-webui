[build]
  publish = "dist"
  command = "npm run build"

# API プロキシ設定 - HTTPSからHTTPのバックエンドへ（ポート80）
[[redirects]]
  from = "/api/*"
  to = "http://34.146.231.219:8000/api/:splat"
  status = 200
  force = true
  headers = {X-From = "Netlify"}

# SPA用のルーティング
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
